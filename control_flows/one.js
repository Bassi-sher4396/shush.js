//if
// 
const balance =1000
if(balance>500) console.log(`fudda maaro`); //multiple lines can also be written without curly parenthesis but thats nt a good practice

const month = 8

switch (month) {
    case 1:
        console.log("january");
        
        break;// breaks the switch case if its true and comes out of switch if not true then goes ahead

    default:
        console.log(`default`);
        
        break;
}